import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("🎲 投骰子")
                .font(.largeTitle.bold())
            Text("版本0.0.25")
            Text("请参考下载包中的完整 iOS 源码。")
        }
        .padding()
    }
}
